#' Linear Mixed Effect Models that cope with censored data.
#' It uses the formula interface of `lme4`-package. As a response variable you can use `Surv`-objects from the `survival` package.
#' @name lme4cens
#' @docType package
#' @importFrom survival Surv
NULL
